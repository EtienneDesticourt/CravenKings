<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="etiennedesticourt.cravenkings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MainMenu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu.OptionsActivity" />
        <activity
            android:name=".Tutorial.StoryActivity"
            android:label="@string/title_activity_story"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Map.MapActivity"
            android:label="@string/title_activity_map" />
        <activity android:name=".Map.SkillsActivity" />
        <activity android:name=".Combat.CombatActivity"></activity>
    </application>

</manifest>